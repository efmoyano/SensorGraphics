<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.unsta.robotteam.sensorGraphics"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        <activity
            android:name="ar.unsta.robotteam.sensorGraphics.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="ar.unsta.robotteam.sensorGraphics.SensorFusion" >
        </service>

        <activity
            android:name="ar.unsta.robotteam.sensorGraphics.SensorConfig"
            android:label="@string/title_activity_sensor_config" >
        </activity>
        <activity
            android:name="ar.unsta.robotteam.sensorGraphics.GraphicConfig"
            android:label="@string/title_activity_graphic_config" >
        </activity>
    </application>

</manifest>